export class Trip {
    constructor(tripName: string, 
                destination: string, 
                dateStart: Date, 
                dateEnd: Date, 
                participants: number) {}
}